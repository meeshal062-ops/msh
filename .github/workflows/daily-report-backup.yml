name: Syrve Sales Report Backup Schedule

on:
  # Backup workflow kept for manual emergency use only to avoid duplicate/blocked schedules.
  workflow_dispatch:

jobs:
  backup-info:
    runs-on: ubuntu-latest
    steps:
      - name: Backup workflow disabled for automatic scheduling
        run: echo "Use the main Syrve Key Metrics Report workflow for scheduled and manual reports."
