name: Syrve Sales Report Backup Schedule

on:
  # Backup workflow is manual only. Automatic scheduling is disabled to avoid duplicate queued/cancelled runs.
  workflow_dispatch:

jobs:
  backup-info:
    runs-on: ubuntu-latest
    steps:
      - name: Backup workflow disabled for automatic scheduling
        run: echo "Automatic scheduling is handled by the main Syrve Key Metrics Report workflow."
